# PKG3 多段跳跃下载实现说明

## 修改概述

实现 Bootloader 支持 PKG3 多段格式，shuaxie.py 分段发送、Bootloader 端最小改动的方案。

## 协议约定

### 下载流程
1. **数据段**：每段独立 `0x34/0x36/0x37`
2. **签名段**：最后单独一段，`addr=0, len=256`（RSA2048_SIG_LEN）
3. **验证**：所有段下载完后发送 `0x31 DFFF` 进行 CRC+签名验证

### 签名段识别
- Bootloader 通过 `addr=0 && len==256` 识别签名段
- 签名段的数据**不参与 CRC 累积**
- 签名写入位置 = `bank_base + accumulated_payload_len`

### CRC/签名范围
- Python 端 CRC/签名只针对 **data 部分**（不含 SEGM 段表）
- Bootloader 端 CRC 只累积 **数据段**（跳过签名段）

---

## 修改文件清单

### 1. pkg_generator.py

#### a) `extract_binary_multisegment()` - 重构 payload 结构
```python
def extract_binary_multisegment(data, base_addr=None, page_size=PAGE_SIZE):
    """
    PKG3 格式 payload 结构（连续写入 Flash，Bootloader 内部解析）：
        [data1] [data2] ... [data_n] [SEGM1] [SEGM2] ... [SEGMn]
    
    SEGM 头（16 bytes）：
        - magic(4):   "SEGM"
        - offset(4):  该段数据在 payload 中的起始 offset
        - data_len(4): 该段数据长度（不含 SEGM 头）
        - reserved(4): 保留
    """
```

**关键改动**：
- 之前：每段前放 SEGM 头 `[SEGM1|data1|SEGM2|data2...]`
- 之后：所有 data 在前，段表在后 `[data1|data2...|SEGM1|SEGM2...]`
- `build_segment_header(offset, data_len)` 中 offset = payload 内 data 起始位置

#### b) `generate_pkg()` - CRC/签名范围修正
```python
# 5. 计算 Payload CRC（只对 data 部分，不含 SEGM 段表）
data_only = payload[0:data_total_len]
payload_crc = calc_crc32(data_only)

# 7. 签名（只对 data 部分做 SHA-256 + RSA，不含 SEGM 段表）
signature = sign_payload(private_key_path, header, data_only)
```

---

### 2. shuaxie.py

#### a) `parse_pkg_file()` - 解析新 payload 结构
```python
# Parse segments for PKG3 (new format: data first, segment table at end)
if magic == b"PKG3" and num_segments > 0:
    seg_table_size = num_segments * 16
    data_total_len = payload_len - seg_table_size
    seg_table_offset = data_total_len
    
    for i in range(num_segments):
        seg_entry_offset = seg_table_offset + i * 16
        data_offset = struct.unpack_from("<I", payload, seg_entry_offset+4)[0]
        data_len = struct.unpack_from("<I", payload, seg_entry_offset+8)[0]
        seg_data = payload[data_offset : data_offset + data_len]
        
        base_addr = payload_addr if payload_addr != 0 else 0x80020000
        flash_addr = base_addr + data_offset
        segments.append((flash_addr, seg_data))
```

#### b) 新增 `_build_34_request()` 和 `_download_single_segment()`
```python
def _build_34_request(addr, length):
    """构建 0x34 RequestDownload 请求数据 (addr=4B, len=4B)."""
    addr_bytes = [(addr >> 24) & 0xFF, (addr >> 16) & 0xFF, 
                  (addr >> 8) & 0xFF, addr & 0xFF]
    len_bytes = [(length >> 24) & 0xFF, (length >> 16) & 0xFF,
                 (length >> 8) & 0xFF, length & 0xFF]
    return [0x00, 0x44] + addr_bytes + len_bytes

def _download_single_segment(uds, seg_offset, seg_data, desc="Seg"):
    """
    下载单段数据：0x34 -> 0x36(blocks) -> 0x37.
    seg_offset: 段在目标 bank 中的偏移地址.
    """
    # 1. RequestDownload (0x34)
    # 2. TransferData (0x36) - send in blocks
    # 3. RequestTransferExit (0x37)
```

#### c) `file_download()` - 重写为多段下载
```python
def file_download(uds, pkg_path):
    """
    多段文件下载（PKG3 格式）。
    每段独立 0x34/0x36/0x37，最后一段为签名（addr=0, len=256）。
    """
    # ...
    for idx, (flash_addr, seg_data) in enumerate(segments):
        seg_offset = flash_addr - base_addr
        _download_single_segment(uds, seg_offset, seg_data, f"Seg{idx+1}")
    
    # Download signature segment
    _download_single_segment(uds, 0, signature_bytes, "Signature")
    
    # CRC + Signature verification (0x31 01 DFFF)
    crc_be = [(payload_crc >> 24) & 0xFF, ...]
    uds_request(uds, 0x31, [0x01, 0xDF, 0xFF] + crc_be, "Verify CRC+Sig")
```

---

### 3. uds_app.c (Bootloader)

#### a) Offset 阈值扩大
```c
// 之前：if (rawAddr < 0x00010000u)
// 之后：覆盖整个 bank 偏移范围
if (rawAddr < 0x00100000u)  // 1MB > 896KB bank size
```

#### b) 新增签名段检测标志
```c
/* Signature segment detection */
static uint8 gs_bIsSignatureSegment = FALSE;
```

#### c) `RequestDownload0x34()` - 签名段检测与累积逻辑
```c
/* Detect signature segment: offset=0, len=256 (RSA2048_SIG_LEN). */
if ((rawAddr == 0u) && (gs_stDowloadDataInfo.DataLen == RSA2048_SIG_LEN))
{
    gs_bIsSignatureSegment = TRUE;
    /* Write signature right after accumulated payload data */
    gs_stDowloadDataInfo.StartAddr = gs_downloadStartAddr + gs_downloadPayloadLen;
}
else
{
    gs_bIsSignatureSegment = FALSE;
    /* On first data segment, save the bank base address */
    if (gs_downloadStartAddr == 0u)
    {
        gs_downloadStartAddr = gs_stDowloadDataInfo.StartAddr;
    }
    /* Accumulate total payload length across all data segments */
    gs_downloadPayloadLen += gs_stDowloadDataInfo.DataLen;
}
```

#### d) `TransferData0x36()` - CRC 只累积数据段
```c
/* Accumulate CRC over payload segments only.
 * Signature segments do NOT participate in CRC.
 */
if ((gs_bCrcActive != FALSE) && (gs_bIsSignatureSegment == FALSE))
{
    gs_DownloadCRC = Boot_CRC32_Update(gs_DownloadCRC,
        &m_pstPDUMsg->aDataBuf[2],
        actualDataLen);
}
```

#### e) 所有错误/成功路径重置多段状态
在以下位置添加状态重置（避免残留影响下次下载）：
```c
// 会话超时/切换时
gs_downloadStartAddr = 0u;
gs_downloadPayloadLen = 0u;
gs_bIsSignatureSegment = FALSE;

// 0x36 序列号错误时
gs_downloadStartAddr = 0u;
gs_downloadPayloadLen = 0u;
gs_bIsSignatureSegment = FALSE;

// 0xDFFF CRC 错误时
gs_downloadStartAddr = 0u;
gs_downloadPayloadLen = 0u;
gs_bIsSignatureSegment = FALSE;

// 0xDFFF 签名错误时
gs_downloadStartAddr = 0u;
gs_downloadPayloadLen = 0u;
gs_bIsSignatureSegment = FALSE;

// 0xDFFF 成功时
gs_downloadStartAddr = 0u;
gs_downloadPayloadLen = 0u;
gs_bIsSignatureSegment = FALSE;
```

#### f) `0xDFFF` 验证使用累积 CRC
```c
/* For multi-segment download (PKG3), use accumulated CRC */
verifyStatus = Boot_DualBank_VerifyBankWithSignature_CrcProvided(
    targetBank,
    expectedCRC,
    gs_DownloadCRC ^ 0xFFFFFFFFu,  /* accumulated CRC */
    signature,
    RSA2048_SIG_LEN,
    gs_downloadPayloadLen);
```

---

### 4. Boot_DualBank.h

#### 新增函数声明
```c
BankStatus_t Boot_DualBank_VerifyBankWithSignature_CrcProvided(
    uint32 bank, uint32 expectedCrc, uint32 actualCrc, 
    const uint8 *signature, uint32 sigLen, uint32 codeSize);
```

---

### 5. Boot_DualBank.c

#### 新增 `Boot_DualBank_VerifyBankWithSignature_CrcProvided()`
```c
BankStatus_t Boot_DualBank_VerifyBankWithSignature_CrcProvided(
    uint32 bank, uint32 expectedCrc, uint32 actualCrc,
    const uint8 *signature, uint32 sigLen, uint32 codeSize)
{
    // Step 1: CRC verification using provided accumulated CRC
    if (actualCrc != expectedCrc)
    {
        return BANK_STATUS_INVALID;
    }
    
    // Step 2: SHA-256 over actual downloaded payload
    uint32 uncachedAddr = (startAddr & 0x00FFFFFFu) | 0xA0000000u;
    __dsync();
    sha256((const uint8 *)uncachedAddr, codeSize, hash);
    
    // Step 3: RSA-2048 signature verification
    // ...
}
```

---

## 测试建议

1. **生成测试 PKG**：
```bash
python pkg_generator.py App_dualBank.aligned.hex 0x00000000 0x00010000 0xFFFFFFFF keys/private.pem test.pkg --base-addr 0x80020000
```

2. **验证段表解析**：
```bash
python -c "from shuaxie import parse_pkg_file; print(parse_pkg_file('test.pkg'))"
```

3. **Bootloader 烧录测试**：
- 擦除目标 bank
- 运行 shuaxie.py 下载
- 验证每段数据是否正确写入对应地址
- 验证 CRC + 签名通过

## 已知限制

- 签名段检测依赖于 `len==256`，如果某段数据恰好也是 256 字节会误判
- 最大支持偏移 0x100000（1MB）
- 段表在 payload 末尾，Bootloader 不解析段表（由 shuaxie.py 负责分段）
