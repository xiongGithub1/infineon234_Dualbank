# BootLoader 跳转导致 App Bus Error Trap 修复记录

**日期**: 2026-05-11  
**问题单号**: 用户现场调试  
**修复人**: AI Assistant  
**关联文件**: `BootLoader_dualBank/AppSw/Tricore/App_bootloader/Boot_DualBank.c`

---

## 1. 问题现象

Bootloader 烧录 App 程序后执行软复位，App 启动运行至 `Boot_CRC32()` 函数返回时（`return crc ^ 0xFFFFFFFFu;`）触发 Trap：

```c
void IfxCpu_Trap_busError(uint32 tin)
{
    // tin = 2 (Data access SRI bus error)
    // trapWatch.tAddr = 0x466 (PSPR0 区域，非法 CSA 地址)
}
```

**关键线索**：使用 UDE 硬件复位后重新运行，App 完全正常。说明 App 代码与数据本身正确，问题出在 **Bootloader 到 App 的切换过程**。

---

## 2. 根因分析

### 2.1 直接原因
`Boot_DualBank_JumpToBank()` 函数中使用 **C 函数调用** 跳转到 App：

```c
void (*appEntry)(void);
appEntry = (void (*)(void))(uncachedStart + 0x20u);
appEntry();   /* <-- 编译器生成 `call` 指令 */
```

TriCore `call` 指令会隐式执行以下操作：
1. 将当前函数的 **upper context** (A10~A15, D8~D15, PSW, PC, PCXI) 保存到 **CSA (Context Save Area)**
2. 更新 `PCXI` 寄存器，使其指向刚保存的 CSA 节点
3. 将返回地址写入 `A11` (RA)
4. 增加 `PSW.CDC` (Call Depth Counter)

### 2.2 崩溃链
- Bootloader 通过 `appEntry()` 调用 App 的 `_start` 后，Bootloader 的上下文被保留在 CSA 链表中（地址约 `0x466` 附近）。
- App 的 `_start` → `main` → ... → `Boot_CRC32()` 执行完毕返回时，需要恢复调用者的 upper context。
- 由于 `PCXI` 仍指向 Bootloader 残留的 CSA 节点，且该地址（`0x466`）并非 64 字节对齐的有效 CSA，硬件在访问时触发 **Data Bus Error (TIN=2)**。

### 2.3 为什么 UDE 复位正常
UDE 执行的是 **System/Application Reset**，CPU 所有核心寄存器（PCXI、FCX、LCX、PSW、A10/A11 等）被硬件复位到默认值。App 从真正的复位状态启动，CSA 链表从零建立，因此不会有历史上下文污染。

---

## 3. 修复方案

### 3.1 核心思路
将 **C 函数调用 (`call`)** 改为 **裸跳转 (`ji`)**，并在跳转前 **手动切断 Bootloader 的上下文链表**。

### 3.2 具体修改

**修改前** (`Boot_DualBank_JumpToBank` 尾部)：

```c
     void (*appEntry)(void);
     // ...
     {
         uint32 uncachedStart = (startAddr & 0x00FFFFFFu) | 0xA0000000u;
         appEntry = (void (*)(void))(uncachedStart + 0x20u);
     }
     /* Jump to APP _START. APP's _Core0_start will reconfigure SP, BIV, BTV, etc. */
     appEntry();
```

**修改后**：

```c
     uint32 entryAddr;
     // ...
     {
         uint32 uncachedStart = (startAddr & 0x00FFFFFFu) | 0xA0000000u;
         entryAddr = uncachedStart + 0x20u;
     }

     /*
      * CRITICAL FIX: Do NOT use C function call (appEntry()).
      * TriCore 'call' instruction implicitly saves upper context into CSA,
      * updates PCXI, A11 (RA) and PSW.CDC. If Bootloader's context is left
      * behind, APP's function returns will corrupt the CSA list and access
      * invalid CSA addresses (e.g. 0x466 in PSPR), causing Bus Error Trap.
      *
      * Correct approach: perform a raw jump (ji) and manually cut the context
      * link chain by clearing PCXI, so APP's _start runs as if from reset.
      */
     /* Load entryAddr to A15 */
     __asm("mov   d15, %0" : : "d"(entryAddr) : "d15");
     __asm("mov.a a15, d15" : : : "a15");
     
     /* Clear PCXI: cut context chain from Bootloader */
     __mtcr(CPU_PCXI, 0);
     __isync();
     
     /* Clear Return Address (A11) */
     __asm("mov.a a11, #0" : : : "a11");
     
     /* Jump indirect: no context save, no link, no RA */
     __asm("ji    a15" : : : "a15");
```

### 3.3 关键动作说明

| 指令 | 作用 |
|------|------|
`mov d15, %0` / `mov.a a15, d15` | 将 App `_start` 入口地址加载到地址寄存器 A15 |
`__mtcr(CPU_PCXI, 0)` | **清空 PCXI**，切断 Bootloader 的 CSA 上下文链表，使 App 认为自己没有上层调用者 |
`__isync()` | 指令同步屏障，确保 `mtcr` 生效后再继续执行 |
`mov.a a11, #0` | 清空返回地址寄存器 A11 |
`ji a15` | **间接裸跳转**，不保存 upper context、不修改 A11、不增加 CDC |

---

## 4. 编译器兼容性处理

本项目使用 **TASKING TriCore v6.2r2** 编译器，修复过程中遇到以下语法差异，已做适配：

| GCC 语法 | Tasking v6.2r2 语法 | 说明 |
|----------|---------------------|------|
| `__asm__ __volatile__(...)` | `__asm(...)` | Tasking 不支持 `__asm__` 和 `__volatile__` 关键字 |
| `"movh.a a15, %H0"` / `"lea a15, [a15]%L0"` | `"mov d15, %0"` / `"mov.a a15, d15"` | Tasking 不支持 `%H0/%L0` split-argument，改用寄存器中转 |
| `"mtcr PCXI, d15"` | `__mtcr(CPU_PCXI, 0)` | Tasking 汇编器不认识 `PCXI` 寄存器名，改用编译器内置函数 |

---

## 5. 影响范围

- **仅修改 Bootloader 的跳转逻辑**，不影响 App 代码、Flash 烧录流程、UDS 协议栈、双 Bank 标志管理等任何其他模块。
- 修改后，`Boot_DualBank_JumpToBank()` 的所有调用点（`Boot_DualBank_SelectAndJump()`、`uds_app.c` 中的 `DoJumpToActiveBank()` 等）均自动受益。

---

## 6. 验证建议

1. **编译验证**：重新编译 BootLoader_dualBank 工程，确认 0 errors。
2. **功能验证**：
   - 通过 UDS 烧录新 App 到 Bank B
   - 执行软复位（`0x11 03` Soft Reset 或 `0x31 02` JumpToApp）
   - 确认 App 能正常启动，且 `Boot_CRC32` 或等效长循环函数返回时不再进 Trap
3. **回归验证**：使用 UDE 硬件复位后运行，确认行为与修改前一致（应无差异）。

---

## 7. 附录：TriCore CSA 上下文机制简述

TriCore 使用 **CSA (Context Save Area)** 链表进行 upper context 的保存与恢复：
- `call` → 保存 upper context 到 `FCX` 指向的空闲 CSA，更新 `PCXI` 指向该 CSA
- `ret`  → 从 `PCXI` 指向的 CSA 恢复 upper context，释放 CSA 回 `FCX`
- `PCXI = 0` → 表示没有前一个上下文，避免回溯到非法地址

Bootloader 直接 `call` App 入口时，Bootloader 的上下文被“嵌入”到 CSA 链表中。App 如果未清空 `PCXI`，其深层函数返回时可能沿链表回溯到 Bootloader 的 CSA 节点，导致内存访问越界或总线错误。

---

# 附录：车企标准化改进记录（BootLoader V3.0.0）

**日期**: 2026-05-11  
**修改人**: AI Assistant  
**关联模块**: `Boot_DualBank`、`Cpu0_Main`、`Tmr`、`uds_app`、`fls_app`

---

## A. 改进概述

按主流车企（OEM）BootLoader 功能安全与诊断追溯要求，对以下模块进行标准化重构：

1. **Boot_DualBank**：验证逻辑、跳转流程、DFlash Flags 结构
2. **Cpu0_Main**：启动时序、看门狗管理、复位原因分析、栈安全
3. **Tmr**：看门狗周期性服务

---

## B. Boot_DualBank 车企标准重构

### B.1 DFlash Flags 结构升级（V2 → V3）

| 字段 | 旧版（40 字节） | 新版（56 字节） | 改进说明 |
|------|----------------|----------------|---------|
| `bankX_valid` | 存储 CRC 值（职责混杂） | 存储 `BANK_VALID_MAGIC` | 避免 CRC=0 被误判 invalid |
| `bankX_crc32` | **无** | **新增** | CRC 值独立存储 |
| `bankX_codeSize` | **无** | **新增** | 精确代码长度，实现**精确 CRC** |
| `flags[7:0]` | 未使用 | 存储回滚原因 | 诊断可追溯 |

**向后兼容**：`Boot_DualBank_ReadFlags()` 自动检测旧版 40 字节格式并**无损迁移**到新版 56 字节。

### B.2 `Boot_DualBank_VerifyBank()` 车企标准验证流程

```
Step 1: 检查 bank 参数合法性
Step 2: 读取 DFlash flags
Step 3: 检查 bankX_valid == BANK_VALID_MAGIC
Step 4: 快速擦除状态检测（采样法）
Step 5: 检查 _START 入口地址合法性
Step 6: 检查 codeSize 范围
Step 7: 计算 [startAddr, startAddr + codeSize] 精确 CRC
Step 8: 与 bankX_crc32 比较
```

- **精确 CRC**：不再固定计算 896KB/1MB，只覆盖实际下载长度
- **CRC 防超时**：`Boot_CRC32()` 每 1KB 喂一次看门狗

### B.3 `Boot_DualBank_JumpToBank()` OEM 标准跳转

| 步骤 | 操作 | OEM 要求 |
|------|------|---------|
| Step 1 | 全局关闭中断 + barrier | 防止跳转中进中断 |
| Step 2 | `CAN_deinit()` + `TMR_deinit()` | 外设去初始化 |
| Step 3 | 禁用 CPU WDT + Safety WDT | 避免 APP 初始化超时复位 |
| Step 4 | 禁用 ECC Trap | 防止新写入 Flash 触发 ECC 陷阱 |
| Step 5 | 清理栈敏感数据（SP → USTACK0） | 信息安全，防止密钥泄露 |
| Step 6 | 关闭 D-Cache / P-Cache + barrier | 缓存一致性 |
| Step 7 | `ji a15` 裸跳转 + `PCXI=0` + `A11=0` | 切断 CSA 上下文链 |

### B.4 `Boot_DualBank_SelectAndJump()` 增强

- 增加详细的**回滚原因记录**到 DFlash flags：
  - `ROLLBACK_REASON_ERASED`
  - `ROLLBACK_REASON_ENTRY_INVALID`
  - `ROLLBACK_REASON_CRC_MISMATCH`
  - `ROLLBACK_REASON_BOOT_TIMEOUT`

---

## C. Cpu0_Main 车企标准重构

### C.1 启动时序（9 步 OEM 标准序列）

```
Step 1: Boot_ReadResetReason()      — 读取并分类复位原因
Step 2: Boot_InitWatchdog()         — 初始化并启用 CPU WDT
Step 3: IfxScuClock_init() ...      — 基本系统初始化
Step 4: Boot_DualBank_Init()        — DFlash flags 初始化
Step 5: RAM Boot Mode 检查           — APP 可请求进入 BootLoader
Step 6: Boot_DualBank_SelectAndJump() — 正常启动时跳转 APP
Step 7: AppBL_init()                — BootLoader 模式初始化
Step 8: Boot_InitStackFill()        — 栈顶填充 0xCC（溢出检测）
Step 9: while(TRUE) + TMR_serviceWdt() — 主循环周期性喂狗
```

### C.2 关键改进

| 改进项 | 原代码 | 新代码 | 说明 |
|--------|--------|--------|------|
| 看门狗 | **禁用** | **启用并周期性喂狗** | 默认超时 ~10.7s |
| RAM Boot Mode | `SelectAndJump()` 在检查前执行 | 先检查 RAM 标志再跳转 | 修复 APP 请求进入 BL 时仍跳转的 bug |
| 复位原因 | 未处理 | 分类 6 种复位原因 | 便于诊断追溯 |
| 栈安全 | 无 | 栈顶 256B 填充 0xCC | 栈溢出检测 |

### C.3 复位原因分类

| 值 | 名称 | 触发条件 |
|----|------|---------|
| 0x01 | `RST_REASON_PORST` | Power-On Reset |
| 0x02 | `RST_REASON_APP_RST` | Software Application Reset |
| 0x03 | `RST_REASON_SYS_RST` | System Reset |
| 0x04 | `RST_REASON_CPU0_WDT` | CPU0 Watchdog Reset |
| 0x05 | `RST_REASON_SAFETY_WDT` | Safety Watchdog Reset |
| 0x06 | `RST_REASON_EXT_RST` | External Reset |

---

## D. Tmr 模块改进

- 新增 `wdtTime` 软定时器变量
- 新增 `TMR_serviceWdt()`：每 **10ms** 调用 `IfxScuWdt_serviceCpuWatchdog()`
- 在 `Cpu0_Main` 主循环中调用

---

## E. 修改文件清单

| 文件 | 修改性质 |
|------|---------|
| `Boot_DualBank.h` | 扩展结构体（56 字节）、新增声明、注释未使用类型 |
| `Boot_DualBank.c` | **完全重写** VerifyBank / MarkBankValid / SelectAndJump / ReadFlags（兼容旧格式） |
| `Cpu0_Main.h` | 删除未使用的 `ResetStatus_Previous` 声明 |
| `Cpu0_Main.c` | **完全重写** `core0_main()`，增加复位原因/看门狗/栈填充/RAM Boot Mode 检查 |
| `Tmr.h` | 新增 `wdtTime` 和 `TMR_serviceWdt()` 声明 |
| `Tmr.c` | 新增 `TMR_serviceWdt()` 实现 |
| `fls_app.h` | 新增 `Flash_GetReceivedDataLength()` / `Flash_GetReceivedDataStartAddr()` 声明 |
| `fls_app.c` | 新增 getter 实现 |
| `uds_app.c` | `0xDFFF` 例程传入 `codeSize` 给 `MarkBankValid` |

---

## F. 编译与兼容性

1. **DFlash Flags 兼容**：旧版 40 字节 flags 上电后自动识别并迁移到新版 56 字节，无需重新刷写 APP。
2. **Tasking 编译器**：所有 `__attribute__((noreturn))`、`__asm` 语法已适配 Tasking v6.2r2。
3. **看门狗超时**：默认 reload=0xFFFC, divBy16384。若 fSPB=100MHz，超时约 **10.7s**。Flash 大容量擦写（>10s）期间建议在擦写函数中加入喂狗。

---
