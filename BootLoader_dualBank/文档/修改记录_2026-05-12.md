# BootLoader Flash 调用链 RAM 化与编译优化调整

**日期**: 2026-05-12  
**修改人**: AI Assistant  
**关联模块**: `Tc234_Modules/Flash`、`Bootloader 编译配置`

---

## 1. 问题现象

刷写 PFlash 过程中程序停在了 `IfxCpu_Trap_contextManagementError`：

```c
void IfxCpu_Trap_vectorTable0(void)
{
    IfxCpu_Tsr_CallTSR(IfxCpu_Trap_memoryManagementError);
    IfxCpu_Tsr_CallTSR(IfxCpu_Trap_internalProtectionError);
    IfxCpu_Tsr_CallTSR(IfxCpu_Trap_instructionError);
    IfxCpu_Tsr_CallTSR(IfxCpu_Trap_contextManagementError);  // <-- 停在这里
    IfxCpu_Tsr_CallTSR(IfxCpu_Trap_busError);
    // ...
}
```

**关键发现**：当前项目使用 **Tasking** 编译器，优化级别为 **`-O0`（无优化）**。

---

## 2. 根因分析

### 2.1 直接原因：Flash busy 期间从 PFlash 取指

TC234 是**单 Bank PFlash** 架构，擦写 PFlash 时不能从同一块 PFlash 执行代码，否则 CPU 取指失败。

虽然项目中已有 `Flash_copyFunctionsToPSPR()` 机制，把部分 iLLD 函数复制到了 PSPR（PSRAM），但**调用链上的大量函数仍然留在 PFlash**：

```
Flash_writePFlash_portex (PFlash)
  └── Flash_writeAndVerifyPage (PFlash)
        └── g_commandFromPSPR.writeFlash → Flash_writePFlashPage (PSPR)
              ├── enterPageMode          (PSPR) ✓
              ├── waitUnbusy             (PSPR) ✓
              ├── writePage              (PSPR) ✓
              └── IfxScuWdt_setSafetyEndinitInline (PFlash!) ← 在 -O0 下未内联
```

在 **`-O0` 无优化** 时：
- 内联函数（如 `IfxScuWdt_setSafetyEndinitInline`）**不会被内联**，生成真实 CALL 指令
- 函数调用层数深，**CSA 消耗大**
- `Flash_writePFlash_portex` **没有关中断**，中断 ISR 若也在 PFlash，Flash busy 时取指失败 → Bus Error → Context Management Error

### 2.2 PSPR 地址冲突隐患

原有 `RELOCATION_START_ADDR = 0x70100000`，与链接器中 `.text.psram_cpu0` section 的起始地址重叠。如果通过 section attribute 把函数链接到 PSRAM，会和运行时 `memcpy` 复制区域冲突。

---

## 3. 修复方案

### 3.1 核心思路

1. **把整个 PFlash 擦写调用链链接到 PSRAM**（`.text.psram_cpu0`），由链接器自动管理，不再依赖运行时 `memcpy`
2. **简化 `Flash_copyFunctionsToPSPR`**，只保留 iLLD 库函数的复制
3. **给 `Flash_writePFlash_portex` 补上中断保护**
4. **把编译优化从 `-O0` 改为 `-O2`**，确保内联函数真正内联，减少 CSA 消耗

---

### 3.2 修改文件清单

#### ① `Flash.h`

**修改内容**：`RELOCATION_START_ADDR` 后移，避开 `.text.psram_cpu0`

```c
/* 修改前 */
#define RELOCATION_START_ADDR       (0x70100000U)

/* 修改后 */
#define RELOCATION_START_ADDR       (0x70101800U)   // 挪到PSPR高地址，避开 .text.psram_cpu0
```

#### ② `Flash.c`

**(a) PFlash 擦写函数全部链接到 `.text.psram_cpu0`**

用 Tasking `#pragma section code "psram_cpu0"` 包裹以下函数：

```c
Flash_erasePFLASH
Flash_writePFlashPage
Flash_writePFlashPage_main
Flash_erasePFlash_port
Flash_writePFlash_port
Flash_writeAndVerifyPage
Flash_ForceWriteRemaining
Flash_writePFlash_portex
```

**(b) 简化 `Flash_copyFunctionsToPSPR()`**

用户自己的函数已由链接器放入 PSRAM，不再需要运行时 `memcpy`：

```c
/* 修改前 */
memcpy((void *)ERASEPFLASH_ADDR, (const void *)Flash_erasePFLASH, ERASEPFLASH_LEN);
g_commandFromPSPR.eraseFlash = (void *)ERASEPFLASH_ADDR;
memcpy((void *)WRITEPFLASH_ADDR, (const void *)Flash_writePFlashPage, WRITEPFLASH_LEN);
g_commandFromPSPR.writeFlash = (void *)WRITEPFLASH_ADDR;

/* 修改后 */
g_commandFromPSPR.eraseFlash = (void *)Flash_erasePFLASH;
g_commandFromPSPR.writeFlash = (void *)Flash_writePFlashPage;
```

**(c) `Flash_writePFlash_portex` 加上中断保护**

原来该函数没有关中断，重构为统一入口/出口：

```c
int Flash_writePFlash_portex(uint32 flashAddr, uint8 *data, uint32 byteLength)
{
    boolean interruptState = IfxCpu_disableInterrupts();
    int result = 1;
    // ... 原有逻辑，所有 return 0 改为 result=0; goto flash_write_exit ...
    
flash_write_exit:
    IfxCpu_restoreInterrupts(interruptState);
    return result;
}
```

#### ③ `.cproject`（编译配置）

**修改内容**：优化级别从 `-O0` 改为 `-O2`

```xml
<!-- 修改前 -->
<option value="com.tasking.ctc.cc.optimize.0"/>

<!-- 修改后 -->
<option value="com.tasking.ctc.cc.optimize.2"/>
```

---

### 3.3 PSRAM 地址布局（8KB）

```
0x70100000 ~ 0x701017FF   ← .text.psram_cpu0（链接器自动分配，约 2~3KB）
0x70101800 ~ 0x701019FF   ← iLLD 手动复制区（erase/wait/unbusy 等，约 1.5KB）
0x70101A00 ~ 0x70101FFF   ← 预留 / 空闲
```

总占用约 **4~5KB**，8KB 完全够用。

---

## 4. 验证方法

1. **编译后检查 `.map` 文件**：搜索上述函数名，确认地址在 `0x7010xxxx` 范围内
2. **功能验证**：重新编译后执行 PFlash 擦写，确认不再进入 Context Management Error Trap
3. **优化验证**：确认编译选项中显示 `-O2`

---

## 5. 附录：今日讨论但未修改的遗留问题

### 5.1 BMHD 配置不一致

`BootLoader_dualBank/IfxCpu_CStart0.c` 中：
- `BMHD_0` 指向 Bootloader (`0xA0000020`) ✅ 正确
- `BMHD_1` 指向 Bank B (`0xA0100020`)，且 CRC 值与 `BMHD_0` 相同 ❌ **不是有效备份**

**建议**：
- 若 `BMHD_1` 作为冗余备份，应改为与 `BMHD_0` 完全一致的 STADBM (`0xA0000020`)
- 若不需要备份，建议删除 Bootloader 工程中的 `BMHD_1`，避免占用 Bank A 起始地址 (`0x80020000`)

### 5.2 DFlash 残留数据干扰启动

`Boot_DualBank_Init()` 在 `Boot_DualBank_ReadFlags()` 返回 TRUE 时直接使用旧数据。若 PFlash 已擦除但 DFlash 未擦除，旧标志会干扰启动判断。

**建议**：在 `Boot_DualBank_Init()` 中加入一致性检查——若两个 Bank 的 reset vector 均为 `0xFFFFFFFF`，强制重新初始化 DFlash flags。

```c
/* 建议增加的空片检测 */
uint32 resetA = *(volatile uint32 *)(BANK_A_START_ADDR + 0x20u);
uint32 resetB = *(volatile uint32 *)(BANK_B_START_ADDR + 0x20u);
if ((resetA == 0xFFFFFFFFu) && (resetB == 0xFFFFFFFFu))
{
    needReinit = TRUE;  /* 强制写入默认值 */
}
```
