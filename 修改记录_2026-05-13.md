# BootLoader FLASH0_FSR 错误标志残留修复

**日期**: 2026-05-13  
**修改人**: AI Assistant  
**关联模块**: `Tc234_Modules/Flash`

---

## 1. 问题现象

一次 PFlash 刷写失败（如电压波动、擦除不完全、命令序列错误等）后，后续的所有刷写操作（包括擦除和写入）都会持续报错失败，即使硬件本身已经恢复正常。

**现场表现**：
- 第一次 `0x36 TransferData` 或 `0x31 RoutineControl` 擦除时偶发失败
- 此后同一台 ECU 再也无法刷写成功，必须断电重启才能恢复
- 诊断仪反复收到 `NRC 0x72 (General Programming Failure)`

---

## 2. 根因分析

### 2.1 核心原因：FSR sticky 错误标志未被及时清除

TC234 的 `FLASH0_FSR`（Flash Status Register）中，错误位是 **W1C（Write-1-to-Clear）** 类型。一旦硬件置位，必须由软件写 1 清除，否则会**永久残留**并影响后续所有 Flash 操作的判断。

### 2.2 代码层面的三个缺陷

#### 缺陷 1：`Flash_writePFlash_portex` 的 `goto flash_write_exit` 绕过 FSR 清除

`Flash_writePFlash_portex` 内部有多条 `goto flash_write_exit` 快速退出路径：
- 缓存拼接写入失败
- `Flash_ForceWriteRemaining()` 失败
- 重叠地址块写入失败
- 主循环中完整 32-byte 块写入失败

这些路径会直接跳转到 `flash_write_exit:`，而原来的代码**只有在函数末尾的 FSR 检查块中才会清除错误标志**。一旦从中间的 `goto` 跳出，FSR 错误位就被**遗留**了下来。

```c
/* 修改前 */
if (Flash_writeAndVerifyPage(...) == 0)
{
    result = 0;
    goto flash_write_exit;  // <-- 直接跳出，FSR 未清除！
}

/* ... */

flash_write_exit:
    IfxScuWdt_serviceCpuWatchdog(wdtPassword);
    IfxCpu_restoreInterrupts(interruptState);
    return result;            // <-- FSR 错误位残留
```

#### 缺陷 2：FSR 清除范围不全面

原有代码只清除被检查的那几个位：
- 擦除：`FLASH0_FSR.U = (1u << 26) | (1u << 13) | (1u << 12) | (1u << 11)`
- 写入：`FLASH0_FSR.U = (1u << 25) | (1u << 13) | (1u << 12) | (1u << 11)`

但 FSR 中还有很多其他 sticky 错误位：
`PFSBER(14)`, `PFDBER(15)`, `PFMBER(16)`, `DFSBER(18)`, `DFDBER(19)`, `DFTBER(20)`, `DFMBER(21)`, `SRIADDERR(22)`, `SPND(27)`, `ORIER(30)` 等。

这些位如果因异常被置位，同样会污染后续操作，却未被原代码清除。

#### 缺陷 3：DFlash 操作完全没有 FSR 检查

`Flash_eraseDFlash_port` 和 `Flash_writeDFlash_port` 直接调用 iLLD 函数，没有任何 FSR 错误检测和清除逻辑。DFlash 用于存储双 Bank 标志位，一旦出错也会影响启动流程。

---

## 3. 修复方案

### 3.1 统一错误掩码 `FLASH0_FSR_ERROR_MASK`

在 `Flash.h` 中定义全面的 sticky 错误位掩码：

```c
/* FSR sticky error flags mask (W1C):
 * OPER(11), SQER(12), PROER(13), PFSBER(14), PFDBER(15), PFMBER(16),
 * DFSBER(18), DFDBER(19), DFTBER(20), DFMBER(21), SRIADDERR(22),
 * PVER(25), EVER(26), SPND(27), ORIER(30) */
#define FLASH0_FSR_ERROR_MASK       (0x4E7F7800U)
```

### 3.2 操作前预清除 FSR（Preventive Clear）

在所有 port 接口函数的**入口处**，先执行一次 `FLASH0_FSR.U = FLASH0_FSR_ERROR_MASK;`，确保历史错误不会干扰本次操作：
- `Flash_erasePFlash_port`
- `Flash_writePFlash_port`
- `Flash_writePFlash_portex`
- `Flash_eraseDFlash_port`
- `Flash_writeDFlash_port`

### 3.3 操作后全量清除 FSR（Curative Clear）

检测到错误返回 `-1/0` 时，不再只清几个位，而是**全量清除**：
```c
FLASH0_FSR.U = FLASH0_FSR_ERROR_MASK;
```

### 3.4 `Flash_writePFlash_portex` 统一出口处理

将 `flash_write_exit:` 改造为**统一出口**：

```c
flash_write_exit:
    /* Ensure all sticky error flags are cleared before leaving */
    FLASH0_FSR.U = FLASH0_FSR_ERROR_MASK;
    if (result == 0)
    {
        s_remainSize = 0;  /* Clear buffer state on error */
    }
    IfxScuWdt_serviceCpuWatchdog(wdtPassword);
    IfxCpu_restoreInterrupts(interruptState);
    return result;
```

这样无论函数从哪个 `goto` 跳出，FSR 都会被清理，`s_remainSize` 也会在出错时重置。

### 3.5 `Flash_writeAndVerifyPage` 增加硬件错误检查

该函数原来只做软件回读验证，现在增加**硬件 FSR 实时检查**：

```c
static int Flash_writeAndVerifyPage(uint32 addr, uint8 *data)
{
    g_commandFromPSPR.writeFlash(addr, (uint32 *)data, 32);

    /* Check hardware error flags immediately after write */
    if (FLASH0_FSR.B.PVER || FLASH0_FSR.B.OPER || FLASH0_FSR.B.SQER || FLASH0_FSR.B.PROER)
    {
        return 0;  /* Hardware flash error detected */
    }

    /* ... software read-back verify ... */
}
```

### 3.6 DFlash 函数补齐 FSR 检查

- `Flash_eraseDFlash_port`：增加 EVER/OPER/SQER/PROER 检查 + 全量清除
- `Flash_writeDFlash_port`：增加 PVER/OPER/SQER/PROER 检查 + 全量清除

---

## 4. 修改文件清单

| 文件 | 修改性质 |
|------|---------|
| `Flash.h` | 新增 `FLASH0_FSR_ERROR_MASK` 宏 |
| `Flash.c` | **全面重构 FSR 错误处理逻辑**（6 个函数） |

### 4.1 `Flash.c` 具体变更函数

| 函数 | 变更内容 |
|------|---------|
| `Flash_writeAndVerifyPage` | 写入后增加 FSR 硬件错误检查 |
| `Flash_erasePFlash_port` | 入口预清除 FSR；错误时全量清除 |
| `Flash_writePFlash_port` | 入口预清除 FSR；错误时全量清除 |
| `Flash_writePFlash_portex` | 入口预清除 FSR；`flash_write_exit` 统一全量清除 + `s_remainSize` 重置 |
| `Flash_eraseDFlash_port` | **新增** FSR 检查与清除 |
| `Flash_writeDFlash_port` | **新增** FSR 检查与清除 |

---

## 5. 验证建议

1. **编译验证**：重新编译 `BootLoader_dualBank`，确认 0 errors / 0 warnings。
2. **功能验证**：
   - 正常刷写完整 App，确认流程通过
   - **故障注入**：在刷写过程中强制断电/断线，模拟一次 FSR 错误
   - 重新上电后再次刷写，确认**无需断电重启**即可成功（即 FSR 错误已被软件自动清除）
3. **DFlash 验证**：执行 Bank 切换、标志位写入，确认 DFlash 错误也能被捕获和清除

---

## 6. 附录：FSR 错误位说明

| Bit | 名称 | 含义 | 是否被原代码清除 |
|-----|------|------|----------------|
| 11 | OPER | Flash Operation Error | ✅（部分） |
| 12 | SQER | Command Sequence Error | ✅（部分） |
| 13 | PROER | Protection Error | ✅（部分） |
| 14 | PFSBER | PFlash Single-Bit Error | ❌ |
| 15 | PFDBER | PFlash Double-Bit Error | ❌ |
| 16 | PFMBER | PFlash Uncorrectable Error | ❌ |
| 18 | DFSBER | DFlash Single-Bit Error | ❌ |
| 19 | DFDBER | DFlash Double-Bit Error | ❌ |
| 20 | DFTBER | DFlash Triple-Bit Error | ❌ |
| 21 | DFMBER | DFlash Uncorrectable Error | ❌ |
| 22 | SRIADDERR | SRI Bus Address ECC Error | ❌ |
| 25 | PVER | Program Verify Error | ✅（部分） |
| 26 | EVER | Erase Verify Error | ✅（部分） |
| 27 | SPND | Operation Suspended | ❌ |
| 30 | ORIER | Original Error | ❌ |

本次修复后，**所有 sticky 错误位均在操作前和操作后得到全面清除**。
